import Layout from "~/components/Layout";

type Props = Record<string, never>;

const Home: React.FC<Props> = ({}) => {
  return (
    <Layout>
      <h1>TopPage</h1>
    </Layout>
  );
};

export default Home;
